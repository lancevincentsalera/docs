import { Callout } from "nextra-theme-docs";

# Playground

<Callout type="info">
  Experiment with your local (or remote) UTxORPC node directly from this page.  
  By default we’re pointing at <code>http://localhost:50051</code>—if your node lives elsewhere, update the <code>endpoint</code> prop on each tab below.
</Callout>

<div style={{ height: '80vh', borderRadius: '0.75rem', overflow: 'hidden', marginTop: '60px' }}>
    <iframe
        src={"/grpcui"}
        title={`gRPC Playground`}
        className="w-full h-[80vh] border-0"
    />
</div>